{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.juniorbay.com/stripe-link/Price.schema.json",
  "title": "Price",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "price_id",
    "currency",
    "quantity"
  ],
  "properties": {
    "price_id": {
      "type": "string",
      "minLength": 1
    },
    "stripe_price_id": {
      "type": [
        "string",
        "null"
      ],
      "description": "Stripe price ID after synchronization. Null for local prices that have not synced yet."
    },
    "previous_price_id": {
      "type": [
        "string",
        "null"
      ],
      "description": "Optional lineage pointer when an archived Stripe Price is replaced."
    },
    "currency": {
      "type": "string",
      "pattern": "^[a-z]{3}$"
    },
    "unit_amount": {
      "type": "integer",
      "minimum": 0,
      "description": "Sale amount charged to the customer in the smallest currency unit."
    },
    "tenant_keyed_amount": {
      "type": "integer",
      "minimum": 0,
      "description": "Amount entered by the tenant before fee-handling adjustments. For net_guaranteed prices, unit_amount is grossed up from this value."
    },
    "compare_at_unit_amount": {
      "type": "integer",
      "minimum": 0,
      "description": "Higher reference amount shown as a struck-through regular price."
    },
    "quantity": {
      "type": "integer",
      "minimum": 1,
      "description": "Quantity represented by this Stripe Price, such as units in a bundle or sessions in a package."
    },
    "badge": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "image_url": {
      "type": "string",
      "format": "uri-reference"
    },
    "context": {
      "type": "string",
      "enum": [
        "standard",
        "sale",
        "flash_sale",
        "upsell",
        "downsell",
        "order_bump"
      ]
    },
    "discount_pct": {
      "type": "number",
      "minimum": 0,
      "maximum": 100
    },
    "pricing_model": {
      "type": "string",
      "enum": [
        "one_time",
        "recurring",
        "customer_chooses"
      ],
      "default": "one_time",
      "description": "How the amount is decided. customer_chooses is a TIP JAR: the buyer picks, so unit_amount is not the answer for it -- presets and/or allow_custom are."
    },
    "min_amount": {
      "type": "integer",
      "minimum": 0,
      "description": "Smallest amount the buyer may pay, in the smallest currency unit. customer_chooses only. Enforced server-side at checkout, never only in the UI."
    },
    "suggested_amount": {
      "type": "integer",
      "minimum": 0,
      "description": "DEPRECATED 2026-09-13 in favour of presets[]. Kept so existing documents still validate; migrate it in as the single preset and stop writing it."
    },
    "recurring": {
      "oneOf": [
        {
          "type": "null"
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "interval"
          ],
          "properties": {
            "interval": {
              "type": "string",
              "enum": [
                "day",
                "week",
                "month",
                "year"
              ]
            },
            "interval_count": {
              "type": "integer",
              "minimum": 1,
              "default": 1
            },
            "trial_period_days": {
              "type": "integer",
              "minimum": 0
            }
          }
        }
      ]
    },
    "fee_handling": {
      "type": "string",
      "enum": [
        "standard",
        "split",
        "net_guaranteed"
      ],
      "description": "Merchant's share of the fees: standard = merchant absorbs all, split = shared 50/50 (buyer price grossed up by half the fees), net_guaranteed = buyer covers all (merchant nets tenant_keyed_amount)."
    },
    "fee_breakdown": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "tenant_keyed_amount",
        "stripe_fee",
        "platform_fee",
        "net_payout"
      ],
      "properties": {
        "tenant_keyed_amount": {
          "type": "integer",
          "minimum": 0
        },
        "stripe_fee": {
          "type": "integer",
          "minimum": 0
        },
        "platform_fee": {
          "type": "integer",
          "minimum": 0
        },
        "net_payout": {
          "type": "integer",
          "minimum": 0
        }
      }
    },
    "metadata": {
      "type": "object",
      "additionalProperties": {
        "type": [
          "string",
          "number",
          "integer",
          "boolean",
          "null"
        ]
      }
    },
    "created_at": {
      "type": "integer",
      "minimum": 0
    },
    "updated_at": {
      "type": "integer",
      "minimum": 0
    },
    "presets": {
      "type": "array",
      "items": {
        "type": "integer",
        "minimum": 0
      },
      "maxItems": 8,
      "description": "Amounts offered as buttons, in the smallest currency unit. CHARGED amounts -- what the buyer pays, inclusive of whatever the fee_handling mode adds, exactly as unit_amount is for a fixed price. customer_chooses only."
    },
    "max_amount": {
      "type": "integer",
      "minimum": 0,
      "description": "Largest amount the buyer may pay. A tenant guardrail for a sensible range, NOT a compliance limit -- platforms do not cap tips. customer_chooses only."
    },
    "allow_custom": {
      "type": "boolean",
      "description": "Whether the buyer may type their own amount alongside the presets. customer_chooses only."
    },
    "allow_recurring": {
      "type": "boolean",
      "description": "Whether the buyer may make the tip recurring. customer_chooses only."
    },
    "recurring_interval": {
      "type": "string",
      "enum": [
        "month",
        "year"
      ],
      "description": "Interval offered when allow_recurring is true."
    }
  },
  "if": {
    "required": [
      "pricing_model"
    ],
    "properties": {
      "pricing_model": {
        "const": "customer_chooses"
      }
    }
  },
  "then": {
    "properties": {
      "unit_amount": {
        "minimum": 0
      }
    }
  },
  "else": {
    "required": [
      "unit_amount"
    ]
  }
}
